#include <unistd.h>

int main(void)
{
	int *psw = (int*) 0x8000;
	int *pled = (int*) 0x8010;
	int count = 64;
	while( 1 )
	{
		usleep (300000);
		if (((*psw) != 0x00) & ((*psw) - 1) > count) count++;
		else count = 0;
		*pled = ~count;
	}
	return 0;
}
